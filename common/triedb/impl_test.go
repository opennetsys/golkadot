package triedb

import (
	"testing"
)

func TestImpl(t *testing.T) {
	// TODO: table tests

	/*
		memdb := db.NewMemoryDB(&db.BaseOptions{})
		txdb := db.TXDB(memdb)
		impl := NewImpl(txdb, []uint8{0x1})

		_ = impl
	*/
}
